source("renv/activate.R")
# Sys.setenv(RENV_PATHS_ROOT = "/scratch/jawirbel/simulation_benchmark/RLIB/renv")
Sys.setenv(RENV_CONFIG_SANDBOX_ENABLED = FALSE)
simba.loc <- '../SIMBA'
temp.loc <- '../temp/'
